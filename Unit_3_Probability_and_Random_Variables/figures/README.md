# Figures for Events and Probability Spaces

These PNG files are used by `events_and_probability_spaces.md`.

Keep the `figures` folder in the same directory as the Markdown file so GitHub can render the images correctly.
